setwd("/Volumes/Macintosh HD/Users/cambrian/Dropbox/scrna-seq/")

library(monocle3)

# load(file = "cds_all.rds",verbose = T)

# cds.all <- preprocess_cds(cds = cds.all,verbose = T)
# cds.all <- reduce_dimension(cds = cds.all,verbose = T)
# cds.all <- cluster_cells(cds = cds.all,verbose = T)

# save(cds.all,file = "cds_all.rds")